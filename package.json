{
  "name": "api-doctor",
  "private": true,
  "scripts": {
    "build:web": "tsc -p tsconfig.json",
    "check:web": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
